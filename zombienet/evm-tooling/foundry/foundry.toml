[profile.default]
src = "src"
out = "out"
libs = ["lib"]
script = "script"
test = "test"
solc_version = "0.8.24"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200

# Networks usable from `forge create --network <name>` style flows.
[rpc_endpoints]
crypto_node = "http://127.0.0.1:9944"
